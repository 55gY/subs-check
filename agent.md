# Agent 规范

## 项目职责

`subs-check` 是订阅和代理节点处理后端。它接收 `tdl-msgproce` 等客户端提交的原始 `sub_url` 和 `ss` 内容，完成解析、校验、Mihomo 转换、节点检测、去重和持久化。

## 解析归属

- 所有协议解析和兼容逻辑统一归属本项目。
- 后端是 `vmess://`、`ss://`、`ssr://`、`vless://`、`trojan://`、`hysteria://`、`hysteria2://`、`tuic://`、WireGuard 及相关格式的唯一解析来源。
- 新增解析前优先复用 `github.com/metacubex/mihomo/common/convert` 和现有 `provider` 能力。
- 只有在 Mihomo 不支持、但 Clash Verge Rev 等成熟客户端支持的格式需要兼容时，才增加自定义兼容逻辑，例如 Shadowrocket 风格的 vmess 内容。
- 必须接受 Bot 提交的原始节点内容，不得要求客户端预先规范化或重新编码。
- 协议转换应尽量放在 `provider` 中，通过公共函数同时服务单节点和订阅解析，避免把转换逻辑写入 HTTP 处理器。
- 兼容处理只修改明确需要转换的目标协议，其他协议必须保持原样。

## API 契约

- `POST /api/config/add` 携带 `sub_url` 时处理远程订阅。
- `POST /api/config/add` 携带 `ss` 时处理一个或多个以换行分隔的原始节点 URI。
- `test` 字段控制添加前是否执行节点检测。
- 返回结构化解析错误，便于客户端展示，但不得泄漏节点密码、API Key 或其他敏感信息。

## 实现规则

1. 解析失败应在后端修复，不得要求客户端增加协议专用临时处理。
2. 单节点和订阅解析必须共享兼容和解析辅助函数，保持行为一致。
3. 修复真实格式问题时增加针对该格式的最小回归测试，不增加无依据的通用解析重构。
4. 在兼容范围内保留旧格式、URL-safe Base64 和无填充 Base64 支持。
5. 日志不得输出完整节点 URI 或解码后的凭据。
6. 修改应保持最小范围，并在提交前执行格式化、测试、静态检查和构建。
7. 未经用户明确要求，不得提交或推送代码。
8. 代码注释、提交信息、变更说明和面向开发者的文档必须使用中文。

## 验证要求

修改后执行：

```bash
gofmt -w <修改过的 Go 文件>
go test ./...
go vet ./...
go build ./...
```

修改解析逻辑时，必须为实际出现的 URI 格式增加回归测试，并验证无关协议仍保持原样。
