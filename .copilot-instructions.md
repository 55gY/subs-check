# Copilot 编码指南

本项目使用以下编码规范：


## 代码风格

1. 保持日志简洁，关键信息突出

## Go 语言特定规范

1. **goto 语句变量声明**：
   - 在使用 `goto` 语句时，必须在 goto 之前声明所有跳转目标后使用的变量
   - 避免 "goto jumps over declaration" 编译错误
   - 示例：
     ```go
     // 错误写法
     if condition {
         goto label
     }
     var x int  // goto 跳过了这个声明
     label:
     
     // 正确写法
     var x int  // 在 goto 之前声明
     if condition {
         goto label
     }
     label:
     ```

## 变更管理规范

**每次代码调整后，必须更新对应的说明文件**：

1. **文档更新要求**：
   - 在完成代码调整后，立即更新相关的说明文档
   - 记录本次调整的目的、影响范围和关键变更
   - 确保文档与代码保持同步

2. **说明文件示例**：
   - 功能更新 → 更新 `README.md`
   - 日志系统变更 → 更新 `LOGGING_UPDATE_STATUS.md`
   - 文件上传功能 → 更新 `FILE_UPLOAD_GUIDE.md`
   - 构建流程变更 → 更新 `BUILD.md`

3. **记录格式建议**：
   ```markdown
   ## [日期] 变更说明
   **调整内容**:
   1. 修复xx
   2. 优化xx

   **影响范围**:
   1. 模块A
   2. 模块B

   **注意事项**:
   1. 注意事项A
   2. 注意事项B
   ```
