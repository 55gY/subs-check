# Copilot 编码规范

本文件用于约束 Copilot 在本仓库中的默认行为。目标是：最小改动、优先修根因、保持现有结构与风格一致。

## 总体原则

1. 优先做小而准确的修改，不做无关重构。
2. 优先修复根因，不做表面兜底补丁。
3. 保持现有包结构、命名风格和公开接口稳定，除非需求明确要求调整。
4. 修改前先理解调用链、并发边界和配置影响范围。
5. 涉及多个文件的改动时，确保配置、文档、前端展示与后端行为同步。
6. 编辑前优先保留原文件备份，避免在文档或配置整理过程中丢失可回退版本。
7. 若用户明确要求“精简 / 整理 / MVP”，默认采用最小展示方案，不额外扩展说明层级。

## Go 代码规范

1. 保持函数职责单一，避免把解析、业务、输出、日志混在同一个函数中。
2. 优先复用现有 package 内能力，不重复造轮子。
3. 错误处理要显式，返回错误时提供足够上下文，但不要冗长。
4. 不随意引入全局状态；必须共享状态时，明确并发安全策略。
5. 非必要不要新增抽象层；能在现有结构中完成的修改，不额外拆接口。
6. 新增导出符号时，命名应与现有风格一致，避免缩写混乱。

## 并发与流水线规范

1. 本项目存在多阶段检测流水线，修改 `checker/`、`provider/`、`core/` 相关逻辑时，优先检查：
   - channel 关闭顺序
   - goroutine 退出条件
   - `context cancel` / 超时传播
   - 统计字段是否重复累计或提前切换阶段
2. 涉及网络请求、测速、媒体检测、IP 查询时，优先沿用 `context` 传递取消信号。
3. 不要引入新的阻塞发送风险；向下游 channel 或结果通道发送前，要考虑取消、超时和消费者退出场景。
4. 调整阶段并发、缓冲区或 worker 数量时，同时检查配置推导逻辑和前端/状态接口展示语义。

## 日志规范

1. 保持日志简洁，关键信息突出。
2. 日志文案优先描述结果和阶段，不输出无意义重复信息。
3. 错误日志要便于定位模块、阶段和失败原因。
4. 不要为正常路径增加过多噪声日志。

## 配置与兼容性规范

1. 修改配置项时，同步更新：
   - 配置解析逻辑
   - 示例配置文件
   - README 或相关说明文档
   - Web/接口中的展示与语义
2. 优先保持旧配置兼容；若必须改变语义，需在文档中明确说明迁移影响。
3. 不要硬编码环境相关值、带宽阈值、URL 或路径，除非仓库中已有明确约定。
4. 若 README 与 `config/config.example.yaml` 存在重复配置说明，优先以 `config/config.example.yaml` 作为唯一详细说明来源；README 只保留配置分组提示和引用入口。
5. 删除或废弃配置项时，必须同步清理代码、示例配置、README、API 文档以及前端/接口中的旧术语，避免残留过时字段名。
6. 对已明确的项目默认值，文档必须与代码保持一致；例如默认 Web 端口使用 `8199`，不得在 README 或示例中写成其他端口。

## 前后端联动规范

1. 修改状态统计、阶段定义、字段语义时，必须同时检查：
   - `core/handlers.go`
   - `core/admin.html`
   - README 中对应说明
2. 前端展示的“进度 / 总量 / 失败数 / 当前阶段”必须与后端实际语义一致。

## 变更管理规范

每次代码调整后，必须同步更新对应说明文件，确保文档与代码一致。

1. 文档更新要求：
   - 记录本次调整的目的、影响范围和关键变更
   - 若变更影响使用方式、配置或输出结果，必须更新 `README.md`
   - 若变更属于阶段性修复，可追加到现有专题说明文档
2. 常见更新位置：
   - 功能、配置、行为变化 → `README.md`（仅保留入口级说明）
   - 详细 API 行为、动态订阅说明 → `API_CHANGES.md`
   - 功能调整/变更历史 → `FEATURE_CHANGES.md`
   - 常见问题 → `FAQ.md`
   - 流水线/并发问题修复 → 对应修复说明文档或统一变更文档
   - 开发约束更新 → 本文件
3. 记录格式建议：
   ```markdown
   ## [日期] 变更说明
   **调整内容**:
  1. 修复xx
  2. 优化xx

   **影响范围**:
  1. 模块A
  2. 模块B

   **注意事项**:
  1. 注意事项A
  2. 注意事项B
   ```

## 修改代码时的执行要求

1. 先搜索现有实现，再决定修改点，避免重复实现。
2. 只改与当前任务直接相关的内容，不顺手修 unrelated 问题。
3. 修改后优先做最小范围验证；有现成构建或测试命令时，先跑与改动最相关的检查。
4. 不要无意义调整格式、导入顺序或重命名大量符号。
5. 本仓库默认验证命令为 `go build ./...`；在每个重要阶段完成后执行一次，确保改动不会破坏构建。
6. 若用户指出编辑器报错或下划线问题，需优先以实际构建结果和错误检查为准，避免被陈旧诊断误导。

## README 与文档整理规范

1. `README.md` 的定位是项目入口文档，优先展示：
   - 一键安装
   - 项目运行流程
   - 主要功能说明
   - 少量常用 API 示例
   - 附加文档入口
2. `README.md` 不应堆积详细变更记录、完整 FAQ、冗长配置讲解或大段 API 细节；这些内容应拆分到独立文档并在 README 中引用。
3. 进行 README 精简时，优先删除重复段落、旧配置名、旧端口、已废弃能力说明，以及与当前实现不一致的示例。
4. 对文档做大段整理后，必须复查文件尾部和章节边界，防止出现重复段落、拼接残留或截断错误。

## Go 特定注意事项

1. `goto` 语句变量声明：
   - 在使用 `goto` 时，必须在 goto 之前声明所有跳转目标后使用的变量
   - 避免 `goto jumps over declaration` 编译错误
2. 对性能敏感路径，避免不必要的字符串拼接、重复解析和重复分配。
3. 修改 goroutine / channel 相关逻辑后，优先关注死锁、提前关闭、阻塞发送和数据竞争风险。
