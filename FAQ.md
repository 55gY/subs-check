# 常见问题

## 1. 如何获取订阅地址？

订阅地址格式为 `http://你的IP:8199/sub?token=你的sub-token`。

获取方式：
- 查看启动日志中自动生成的 `sub-token`
- 在配置文件 `config.yaml` 中查看 `sub-token` 字段
- 启用 Web 面板后，通过 API Key 验证后在面板中查看

## 2. 订阅返回 404 怎么办？

可能原因：
- `token` 参数缺失或错误
- 同一 IP 连续失败 3 次已被封禁 30 天
- 暂无可用节点（检测尚未完成或无匹配结果）

建议：
- 检查 URL 中 `token=` 参数是否与配置文件中 `sub-token` 一致
- 等待 30 天封禁自动解除，或清除服务器上的数据库文件重新开始
- 确认检测任务已完成且有可用节点

## 3. Web 面板显示"验证 API 密钥后显示"？

这是正常的安全行为。在面板中输入正确的 API Key 后，订阅地址和配置路径会自动显示。

## 4. 为什么鉴权失败返回 404 而不是 401？

返回 404 而非 401/403 是故意的安全设计，目的是避免泄露接口存在性和认证方式。

## 5. 程序在测速阶段异常终止？
可能原因：
- 内存不足
- 并发过高
- 个别节点触发异常
建议：
- 启用软内存限制：`SUB_CHECK_MEM_SOFT_LIMIT=2GB`
- 降低 `concurrent-stage`
- 开启内存监控：`SUB_CHECK_MEM_MONITOR=1`
## 6. 节点检测速度太慢？
建议：
- 提高合适的并发数
- 减少或关闭媒体检测
- 更换更稳定的测速地址
- 使用 `success-limit` 控制输出规模
## 7. 某些订阅无法获取？
建议：
- 检查订阅链接是否有效
- 配置 `sub-urls-retry-failed`
- 查看 `sub_failure_record.txt`
- 查看运行日志定位错误
## 8. 如何通过 API 添加订阅链接？
使用 `POST /api/config/add`，详见 [API_CHANGES.md](API_CHANGES.md)。
## 9. 智能失败处理如何避免误删订阅？
核心机制是失败计数、成功重置以及本地/远程订阅分级处理，详见 [API_CHANGES.md](API_CHANGES.md)。
## 10. API 密钥在哪里查看？
可在启动日志中查看自动生成的密钥，或在配置文件中通过 `api-key` 自定义，也支持环境变量 `API_KEY`。
## 11. Web 服务无法访问？
建议检查：
- 端口是否被占用
- 防火墙是否放行
- `listen-port` 是否正确
## 12. 配置文件修改后不生效？
项目支持热重载；若未生效，请检查：
- YAML 语法是否正确
- 日志中是否有报错
- 是否修改了需要重启才能生效的配置项
## 13. 如何限制测速流量？
使用 `total-speed-limit`，具体写法见 [config/config.example.yaml](config/config.example.yaml)。
## 14. 节点去重不起作用？
去重基于连接信息而不是节点名称；名称不同但连接信息相同的节点仍会被识别为重复节点。
